# 20260810
if문에 대해서 배우고 비교 연산 문제를 푸는데, 정수 A,B 를 입력받아서 같거나 큰가, 큰가, 작거나 큰가, 작은가,
다른가 등의 문제에 대해 print하는 문제였다. 나는 생각치도 못했는데 같은 ssafy 동료인 강준서양의 풀이는
print(int(A >= B)) 등으로 비교 연산 값인 boolean 데이터를 int로 명시적 형변환해서 출력하는 형태였다.
꼭 if문을 쓰지 않아도 풀 수 있다는 것을 배웠다. 

함수 호출 call
return 반환값

## 함수구조
input 재료 (파라미터)
들여쓰기 (한 세트임)

함수구조(2/2)
p12
함수는 인풋과 아웃풋이 존재한다. 
실행하는 예시를 설명해서 쓴것 - 왼쪽 코드 여러줄 주석

함수 정의와 호출(1/5) p13
def 함수 이름 - 변수명 짓기와 같음 언더바
동사형태의 이름을 가지면 좋다
매개변수는 가로열리고 닫히고 인풋이 없는 경우도 있다

p14
함수의 바디 끝날때 :콜론 있음.
알아서 들여쓰기로 vscode가 안내
함수 실행시 바디 부분이 실행됨

p15
선택적

p16
필요한 경우 
return 주고 
return 만나면 종료된다 결과를 호출
반환값이 없는 함수는 알아서 return None 구문을 반환함
'함수 내에서 return 문이 없다면 none이 반환됨'

p17
05_funtions
01_basic.py

def make_sum(pram1, pram2):
    """이것은 두 수를 받아
    두 수의 합을 반환하는 함수입니다.
    >>> make_sum(1,2)
    3
    """

p19
1번째 난관

## 매개변수와 인자

p21
함수를 정의할 때 매개변수 x,y
함수를 호출할 때 인자 a,b

## 다양한 인자의 종류
2번째 난관

p24
위치 인자

1. 제대로함
2. 함수를 제대로 보지 않음
3. 한 필수 위치 인자가 누락되었습니다라는 타입에러뜸

p25
기본 인자 값
age=30은 기본 인자 값
인자를 재대로 쓰면 그거 활용함 40

p26
키워드 인자
name = 'dave', age = 35 ...
인자 이름 값
순서는 중요하지 않음
주의사항 마지막줄 에이지 매칭될꺼니까 이름은 대충?
키워드 어큐먼트 뒤에는
위치 인자 2개인데 키워드 인자(위치 인식 안됨), 위치 인자
그러면 dave가 어떤 위치에 보장해?
위치 인자가 앞에 위치해야 보장할 수있음

p27
순서의 모호성
키워드 인자는 순서파괴 후 이름 지정
이 값이 몇 번째 자리에 들어가야 하는지 

p28
임의의 인자 목록

print(12, 3, 4, 100, 'asdasd')
이거 됨
프린트 함수는 어떻게 몇개 인자가 들어올지 예상을 하지
tuple로 처리
임의 인자를 출력하도록?

p29
딕셔너리

p30
다쓰는 함수를 만든다면 
위치 기본 가변 가변키워드 순으로 작성하자
모든 상황을 다 쓴다면 

p31
다쓴 예시
print(*objects, sep=' ', end='\n', file=None, flush=False)

## 재귀함수
p33
교안

p34
팩토리얼 하나씩 풀어보기
4! = 4 * 3! = 4 * 3 * 2!

p35
5 * = f(4)
4 * f(3)
...

p36
호출 방향 
factirial(1) = return 1
너 쌓여있잖아 호출이 없을떄까지
바구니를 비워야함. ??? 메모리뭐시기 - 스택이 쌓인다.
반환 방향 반대임

p37
베이스 케이스 수렴

p38
1개 이상의 종료 조건이 있고 수렴해야함
종료 조건을 명확하게 해야함
반복되는 호출이 종료 조건을 향하도록 함
재귀함수는 메모리 사용량 많음
스택 오버플로우 감당할 수 있는것 이상으로 쌓임
오히려 속도가 안나올수도
특정 문제가 재귀로 풀면 난이도가 쉬운 경우가 있음

p39
훨씬 간결하게 풀릴 수 있음
재귀함수 정의 함수 내부에서 자기 자신을 호출하는 함수

## 내장 함수
p41
print 등 
파이썬 공식 문서 내장 함수
외장 함수 라는 용어는 공식적으로 없어요
영어원문 일어 한국어 이 과정에서 내장 외장이 번역됨
영어 원문에는 외장함수 없음 그냥 함수임

p42

## 함수와 scope

p44
함수 내외부

p45
num 이란 변수 없는데?
local scope 안에 있는 변수
global scope에는 정의되지 않음
num 수명이 함수 안에서 끝남

p46
1. 빌트인 스코프
2. 전역 스코프
3. 로컬 스코프 
   아까의 num

p47
이름이 없으면 찾아나가는 영역 LEGB
안쪽으로는 못 찾고 바깥쪽에서 찾아옴

p45
안쪽으로는 못찾음

함수 바깥의 변수는 접근 가능하나 수정은 불가

p48
sum 함수 이제 못써!
sum은 이제 정수야 인트 객체는 불러올 수 없어!
LEGB 룰에 의해 G에 sum을 쓰면 B에서꺼는 무시
빌트인 다가가지 못해서 sum 함수 불러올 수 없음

49
50
51
읽어보자

## global 키워드

53
increment 
이제 부터 전역 변수 선언할거다 global num
전역 변수를 컨트롤해야하는 경우

54
키워드 주의사항 -2는 파이썬 버전때문에 다르다

## 함수 스타일 가이드

## 함수 이름 규칙

57
함수의 이름은 길어도 괜찮다
긴거 보다 중요한건 직관적 명시적?
명확하게 쓸수 있도록 

58
is_loggedin 아 얘는 true false로 반환하겠구나
tip 2번째 줄

## 단일 책임 원칙

60
하나의 명확한 목적과 책임만을 가짐
1. 명확한 하나의 작업 수행
단일 함수 목적 하나씩 있어야 조합할 수 있다
문제가 있어도 해결 가능
하나가 역할이 많으면 고장나도 고치기 힘들어
책임분리한다

2. 책임 분리
검증, 처리, 저장 3개로 나누어서 함수 분리

3. 유지보수성
수정 시 영향 범위 최소화

61
잘못된 설계 예시
검사, 암호화 및 저장, 발송
좋지 않은 함수

62
책임 분리
마지막 메인함수에서 블럭쌓기

61이 쉽고 62처럼 어려운 작업이 
책임 하나씩 분할해줄수 있어? 제미나이 질문하자!

## 패킹 언패킹

패킹

65
포장하는거

66

언패킹

70
71

73
패킹 정의
언패킹 호출 

## 참고

함수와 반환
76
단 하나의 튜플을 반환하는 것이다
튜플로 패킹되어서 여러 값을 반환하는 것처럼 보이나 하나만 반환

77
## 람다 표현식

79
익명 함수
두개가 같은 함수이다
def 이름이 없다 그냥 람다에 매개변수 표현식으로 나온다
모든 함수를 람다로 만들 수는 없다
이름을 재사용하고 싶어서?
이름을 없앴다는거? 1회용으로 쓰고 싶어서 지금 잠깐 쓰고 싶어

80
sorted 내장 함수
이름과 나이 형태로 묶여있는 리스트
왼쪽 코드의 get_age 함수를 lambda 익명 함수로 바꿈

# =============================================================
# as 키워드 - 이름 충돌을 해결하는 별칭(alias)
# - 가져오는 이름에 내가 원하는 다른 이름을 붙일 수 있음
# - 앞 파일에서 서로 덮어쓰던 두 sqrt가 이제 각각 살아남음
# - 이름이 길거나 헷갈릴 때 짧게 줄이는 용도로도 자주 사용
#   예) import pandas as pd
#
# =============================================================

from math import sqrt

from my_math import sqrt as my_sqrt  # 별칭으로 공존

sqrt(4)  # math
my_sqrt(4)  # my_math

# =============================================================
# [1] 변수(Variable) 할당
# - 변수명 = 값
# - '='는 같다는 뜻이 아니라 '오른쪽 값을 왼쪽 이름에 넣어라'는 의미
# =============================================================

degrees = 36.5

# =============================================================
# [2] 재할당(Reassignment)
# - 같은 변수에 새로운 값을 다시 넣을 수 있음
# - 파이썬은 자료형이 달라져도 재할당 가능 (동적 타이핑)
#   => degrees는 실수(float)였다가 문자열(str)로 바뀜
# =============================================================

# 재할당
degrees = 'hello'

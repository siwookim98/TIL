"""
빈 변수 만들기 + 2차원 배열 + 2중 for + 조건문 (스켈레톤)
====================================================================
빈칸(TODO)을 채우며 '빈 상자를 만들고 -> 반복 돌며 -> 조건 맞는 것만 담기'를 익힌다.

예제 데이터 (2행 3열):
        열(j) ->   0    1    2
    행(i) 0        1    2    3
    행(i) 1        4    5    6

** 코드를 실행하기 전에 반드시 지킬 것 **
    1) 위 표를 종이에 똑같이 그린다.
    2) 아래 '손 추적표'를 코드 실행 '전에' 손으로 먼저 채운다.
       (최소 i=1로 넘어가는 순간까지 = 2바퀴는 반드시!)
    3) 그 다음 코드를 실행해서, 내가 적은 것과 실제 출력이 같은지 검산한다.

--- 손 추적표 (예시 2용: 짝수만 골라 evens 에 담기) : 직접 채우세요 ---
  +-----+-----+--------+---------+---------------------+
  |  i  |  j  | value  | 짝수?   | evens (담은 직후)   |
  +-----+-----+--------+---------+---------------------+
  |  0  |  0  |        |         |                     |
  |  0  |  1  |        |         |                     |
  |  0  |  2  |        |         |                     |
  |  1  |  0  |        |         |                     |   <- j가 '다시 0'!
  |  1  |  1  |        |         |                     |
  |  1  |  2  |        |         |                     |
  +-----+-----+--------+---------+---------------------+

* 참고: 각 함수는 지금 실행하면 제목만 출력됩니다.
  TODO 를 채우고 맨 아래 'pass' 줄을 지우면 완성됩니다.
"""

matrix = [
    [1, 2, 3],
    [4, 5, 6],
]


# =============================================================================
# 예시 1) 2차원 배열 전체 순회 -- i(행), j(열)로 모든 칸 방문
#   목표 출력:
#       matrix[0][0] = 1
#       matrix[0][1] = 2
#       matrix[0][2] = 3
#       matrix[1][0] = 4   <- i가 1로 넘어가면 j는 '다시 0부터'!
#       matrix[1][1] = 5
#       matrix[1][2] = 6
# =============================================================================
def example_1_visit_all():
    print('===== 예시 1) 전체 순회 =====')

    # [실습 포인트] 바깥 for 문: 행 번호 i. 행의 개수는 len(matrix) 다.
    # TODO: for i in range(...): 로 바깥 반복문을 시작하세요.

    # [실습 포인트] 안쪽 for 문: 열 번호 j. (바깥 for 블록 '안에' 들여쓰기!)
    #   i번 행의 칸 수는 len(matrix[i]) 입니다.
    # TODO: 안쪽 for 문을 작성하세요 (변수명: j)

    # [실습 포인트] 가장 안쪽에서 matrix[i][j] 값을 출력하세요.
    # TODO: 'matrix[i][j] = 값' 형태로 print

    pass  # <- TODO 를 다 채우면 이 줄은 지우세요


# =============================================================================
# 예시 2) 빈 리스트를 만들어 '짝수만' 골라 담기 (누적 패턴)
#   목표 출력: [2, 4, 6]
# =============================================================================
def example_2_accumulate():
    print('===== 예시 2) 빈 리스트에 쌓기 =====')

    # [실습 포인트] ★가장 중요★ 반복을 돌기 '전에' 담을 상자를 스스로 만든다.
    #   빈 리스트는 어떻게 만들까? (힌트: 대괄호 [])
    # TODO: 빈 리스트 evens 를 만드세요.

    # [실습 포인트] 예시 1과 같은 2중 for 구조로 표를 훑는다.
    # TODO: 바깥 for (i) / 안쪽 for (j) 를 작성하세요.

    # [실습 포인트] value = matrix[i][j] 를 꺼내고,
    #   '짝수'(2로 나눈 나머지가 0)일 때만 evens 에 담는다. (리스트에 담기: .append())
    # TODO: 조건을 통과한 value 만 evens 에 append 하세요.

    # [실습 포인트] 반복이 끝난 뒤, 상자에 무엇이 모였는지 확인한다.
    # TODO: evens 를 출력하세요.

    pass  # <- TODO 를 다 채우면 이 줄은 지우세요


# =============================================================================
# 예시 3) print 검산 -- 상자(evens)가 채워지는 과정을 눈으로 본다
# =============================================================================
def example_3_print_debugging():
    print('===== 예시 3) print 검산 =====')

    # [실습 포인트] 빈 상자를 만들고, 시작 시점의 상태를 먼저 찍어둔다.
    # TODO: evens = [] 를 만들고 '[확인] 시작할 때 evens = {evens}' 출력

    # [실습 포인트] 바깥 for (i) 안에서, 새 행 진입을 찍으면 'j 리셋'이 보인다.
    # TODO: 바깥 for 문 + '[확인] {i}번 행 진입 -> j를 0부터 다시 시작' 출력

    # [실습 포인트] 안쪽 for (j) 에서 매 칸의 i, j, value 를 찍어 추적표와 대조.
    # TODO: 안쪽 for 문 + value 꺼내기 + '[확인]    i={i}, j={j}, value={value}' 출력

    # [실습 포인트] 짝수면 evens 에 담고, 담은 '직후' 상자 상태를 찍는다.
    # TODO: 짝수일 때 append + '[확인]    -> 짝수! evens = {evens}' 출력

    # TODO: 반복이 끝나면 '최종 결과: {evens}' 출력

    pass  # <- TODO 를 다 채우면 이 줄은 지우세요


# =============================================================================
# 실행부: 실습할 예시만 주석을 해제해서 하나씩 실행하세요.
# =============================================================================
if __name__ == '__main__':
    example_1_visit_all()
    # example_2_accumulate()
    # example_3_print_debugging()
